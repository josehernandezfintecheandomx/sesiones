import { MayusculaDirective } from './mayuscula.directive';

describe('MayusculaDirective', () => {
  it('should create an instance', () => {
    const directive = new MayusculaDirective();
    expect(directive).toBeTruthy();
  });
});
